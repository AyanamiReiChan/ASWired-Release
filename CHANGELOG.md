# v1.0.7

- 流量分析新增“内部中转”和“未归属”明细，展示节点、账号标识及上传/下载用量；管理员可按具体节点和账号登记线路名称、来源节点，并撤销分类。
- 已确认的内部中转流量从成员“未归属”汇总中单列，历史记录按当前分类展示，保留原始台账和服务器用量，不重复扣除成员订阅额度。没有登记的未知账号继续显示为未归属。
- 已登记内部账号的新上报不再产生“找不到订阅”的缺口或无流量记录，仍保留核心重启、计数回退等真实缺口；历史缺口标记保持原样。
- 增加分类权限、同名账号跨节点隔离、历史展示、增量同步和计费不变验证。升级保留现有账户、套餐、配置、密钥及流量台账；远端 Agent 无需升级。

# v1.0.6

- 修复自定义监听端口导致“升级成功但显示失败”：升级后的健康检查读取实际部署配置，不再固定检查前端 3000 等默认端口。
- 复核目标版本目录、运行中的主控/Komari/前端程序、网站目录、服务定义和 HTTP 响应；服务未启动、版本不符或配置未完整切换时仍保留失败，不能仅凭版本号标记成功。
- 兼容旧更新脚本：升级至本版本后，新的结束处理程序会按实际配置复核旧脚本留下的失败记录，通过全部检查才显示完成。无需修改现有监听端口或反代配置。
- 当主控已经运行目标版本、但升级检查仍未通过时，页面明确区分两种状态。新增自定义端口、旧状态复核及服务故障回归验证。

# v1.0.5

- 证书管理显示主控和 Komari 当前实际 HTTPS 证书，校验域名、系统信任链、有效期与指纹，明确区分 Caddy、外部反代和主控管理。
- 新增外部 HTTPS/反代模式及本机 Caddy 接管：申请或上传证书后可部署到主控、Komari 或两者，支持 DNS 提供商、根域名与泛域名、自动续期和自动部署。
- 独立低权限请求与 root 部署服务校验证书材料、热加载并核对实际生效指纹；保留路由和 TLS 约束，持久化重启配置，失败回退，支持交回外部管理。
- 从 v1.0.4 自动升级会启动新证书监听服务；默认保留外部模式，不自动替换已有网站证书。Nginx/其他反代继续使用外部管理，详见 docs/WEBSITE-CERTIFICATES.md。
- 发布检查增加真实 Caddy 双站点切换、重启保留、权限边界、失败回退、旧更新流程激活服务，以及前后端证书回归验证。

# v1.0.4

- 修复部分 Debian 12 / Python 3.11.2 安装在解压阶段自更新失败的问题：使用独立安全解压器，不再依赖 `tarfile.data_filter`。保留 SHA256 校验，拒绝越界路径、危险文件、重复路径及向目录外的链接，只在新建临时目录中解压。
- 增加 Python 3.11.2 / 3.12 回归和实际发行包解压检查；AMD64 / ARM64 安装测试均经过新的解压器。
- 包含 v1.0.3 的主控 CPU 优化，主控、网站、Agent、Komari 源提交与 v1.0.3 一致；本版只修改发行包更新脚本。远端 Agent 无需升级。
- 如果旧版更新器已经因缺少 `tarfile.data_filter` 失败，须先修补当前安装的更新脚本和解压器；直接重复点击升级不会解决旧脚本的兼容性问题。操作说明见 `docs/UPGRADE.md`。

# v1.0.3

- 降低 SQLite 主控流量统计开销：台账未变化时复用精确汇总，台账事务提交后立即失效；移除统计与上报路径中的重复建表/索引检查。
- 流量上报只刷新有新台账的订阅，维护任务补偿显示刷新失败。保留原有计费倍率、周期边界、配额/超额限速、零增量行为检测和 5 秒维护周期。
- 新增事务回滚、外部连接写入、并发快照、浮点逐位一致、旧库升级、备份恢复和配额立即生效回归；PostgreSQL 继续使用原始汇总查询。
- 本地合成基准（82,203 条台账、9 个订阅）：台账未变化时，整轮到期检查从约 367 ms 降至约 0.8 ms。该结果不代表线上 CPU 百分比；持续变化的台账仍会重新计算。方法及限制见主控仓库 `docs/traffic-performance.md`。
- 支持从 v1.0.2 的「设置 → 系统维护」自更新，保留账户、套餐、配置、密钥及流量台账。新增的派生版本表由主控启动时创建；不需要修改 Agent 上报参数或升级远端 Agent。
- Linux amd64 / arm64 部署包固定源提交并附带 SHA256 校验和；网站、Agent 和 Komari 源码与 v1.0.2 相同，组合制品版本统一标记为 v1.0.3（Komari 1.2.5-fix2-aswired.1.0.3）。

# v1.0.2

- 主控新增网页自更新：管理员确认 GitHub 目标版本后，由独立 root 更新服务执行下载校验、数据备份及整套服务切换；页面显示进度与结果，主控保留低权限运行。旧安装须先升级一次并启用更新服务。

- 修复 Agent 首次启动缺少 Xray 配置，导致核心无法启动、配置读取及同步失败的问题。自动创建无监听端口的基础配置，保留已有配置并拒绝不安全的文件路径。
- Agent 升级页增加全选可升级节点、清空选择、选中数量及不可选原因；提交前重新检查节点状态，避免重复下发未完成的升级。
- 「探针监控」及旧 `/probe` 链接直接跳转到配置的 Komari 公开首页，保留「Komari 管理」统一登录入口。
- Komari Agent 手动安装和自动发现命令默认使用 5 秒上报间隔，仍支持自定义；不会自动修改已有探针的运行参数。
- 配套 Komari 1.2.5-fix2-aswired.1.0.2，提供 Linux amd64 / arm64 部署包和独立 Agent 制品，固定源提交并附带 SHA256 校验和。

# v1.0.1

- 修复 Clash/Mihomo 订阅端口被序列化为字符串导致导入失败的问题，模板预览和下载同步修复。
- 套餐默认 Clash 模板新增自动测速、手动选择和 AI、媒体、游戏、国内直连及广告拦截等 22 个分组，内置 10,251 条去重规则；仅填入授权节点，已有自定义模板优先。
- 订阅生成器改为先选择套餐、再选择成员，默认沿用套餐模板；保留自定义规则和显式模板模式。移除成员订阅页底部的两张说明卡片。
- 纳入主控、浏览器和 Agent 的增量同步与独立心跳改进，减少重复数据传输。
- 同步整合版 Komari 的管理员权限及流量单位显示修复，配套版本为 1.2.5-fix2-aswired.1.0.1。
- Linux amd64 / arm64 部署包固定全部源提交，附带 SHA256 校验和。升级保留账户、套餐、密钥和环境配置；远端 Agent 仍由管理员单独选择升级。

# v1.0.0

- ASWired 网站、主控和管理 Agent 的首个公开组合发行版，Linux amd64 / arm64。
- 配套 Komari 1.2.5-fix2-aswired.1.0.0，统一身份库；首次管理员由部署者在网页自行设置。
- 默认毛玻璃主题；服务器、节点、用户、套餐、订阅生成与管理、模板、证书、转发及数据库管理。
- 管理 Agent 支持 WebSocket / HTTP / Pull / 自动回退，内嵌 Xray；主机监控仅由 Komari 提供。
- Agent 日志默认手动读取 100 行；GitHub Release 版本检查、固定版本下载、SHA256 校验和 Agent 回退监督。
- 仓库以审核后的干净历史首次公开；内部部署记录、真实部署地址及私有运行数据不进入发行版。

协议和部署限制见 README；源代码测试、Linux 构建、首次初始化和服务安装检查的结果以对应 GitHub Actions run 为准。
